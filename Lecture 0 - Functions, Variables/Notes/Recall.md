# CS50P Lecture 0 — 5-Minute Summary

Today in CS50P Lecture 0, I learned the basics of Python, starting from `print()` and moving into data types, basic calculations with `int` and `float`, and functions.

## What surprised me

What surprised me the most was how many options there are even for something simple like `print()`.

For example, changing `sep` and `end` made me realize that programming often gives you many ways to do the same thing.

That also made me wonder:

> If there are so many ways to solve a problem, how do I know which way is the “right” or most optimal one?

## Comparing CS50P with Angela Yu’s course

Compared to Angela Yu’s course, CS50P felt more in-depth.

It explained small details that I didn’t think about before, like how `print()` can be customized instead of just used in the basic way.

## Functions

The part I liked the most was functions.

I especially liked how values can be passed into a function through the parentheses, processed inside the function, and then sent back using `return`.

That helped me understand that functions can communicate with each other instead of just doing one isolated task.

## Something I searched by myself

I also searched something by myself:

Variables can start with `_` in Python, like this:

```python
_name = "Tirzah"
print(_name)